const el = document.querySelector('.new-element');

const clone = el.cloneNode(true);

/* create a deepclone of any domnode  */

/* No need to create same element and set all the properies again and again */
